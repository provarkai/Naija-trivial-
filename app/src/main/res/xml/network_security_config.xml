<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Cleartext HTTP is only permitted to these dev-only hosts, so you can
         point ai4biz.backend.url at a server running on your own machine
         (10.0.2.2 is the Android emulator's alias for the host loopback)
         without deploying anything. Everything else stays HTTPS-only. -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
